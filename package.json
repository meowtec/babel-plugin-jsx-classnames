{
  "name": "babel-plugin-jsx-classnames",
  "version": "1.0.1",
  "description": "babel plugin for automatically adding `classnames()` to `className` attribute.",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src -d lib --presets env",
    "prepack": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/helper-module-imports": "^7.0.0-beta.54",
    "@babel/plugin-syntax-jsx": "^7.0.0-beta.54",
    "classnames": "*"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0"
  }
}
